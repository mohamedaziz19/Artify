import React from 'react';
import ReactDOM from 'react-dom/client';
import WildSoul from '../Artify.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WildSoul />
  </React.StrictMode>
);
